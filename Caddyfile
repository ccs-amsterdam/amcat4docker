{$SITE_ADDRESS} {
    # Route API requests to the amcat4 container
    handle_path /amcat* {
        reverse_proxy {$AMCAT_API}
    }

    # Route everything else to the frontend GUI
    handle {
        reverse_proxy {$AMCAT_UI}
    }

    # Optional: compression for better performance
    encode zstd gzip
}
